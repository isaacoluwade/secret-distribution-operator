---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: exportsecrets.secrets.mtkp.platform
spec:
  group: secrets.mtkp.platform
  names:
    kind: ExportSecret
    listKind: ExportSecretList
    plural: exportsecrets
    singular: exportsecret
    shortNames:
      - es
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: TargetPath
          type: string
          jsonPath: .spec.targetPath
        - name: LastSynced
          type: date
          jsonPath: .status.lastSyncedTime
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion: { type: string }
            kind: { type: string }
            metadata: { type: object }
            spec:
              type: object
              required: [sourceSecretName, targetPath]
              properties:
                sourceSecretName:
                  type: string
                  pattern: '^[a-z0-9][a-z0-9.-]{0,251}[a-z0-9]$'
                targetPath:
                  type: string
                  pattern: '^platform/(dev|staging|prod|prod-dr)/[a-z0-9-]+/[a-z0-9-]+$'
                kmsKeyArn:
                  type: string
                tags:
                  type: object
                  additionalProperties:
                    type: string
            status:
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    required: [type, status, lastTransitionTime, reason, message]
                    properties:
                      type: { type: string }
                      status: { type: string, enum: ["True","False","Unknown"] }
                      reason: { type: string }
                      message: { type: string }
                      lastTransitionTime: { type: string, format: date-time }
                      observedGeneration: { type: integer, format: int64 }
                lastSyncedTime: { type: string, format: date-time }
                observedGeneration: { type: integer, format: int64 }
                secretArn: { type: string }
                versionId: { type: string }
